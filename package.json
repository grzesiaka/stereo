{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "stereo",
  "version": "777.777.777",
  "description": "mono stereo",
  "keywords": [],
  "license": "NON-AI-MPL-2.0",
  "author": "",
  "type": "module",
  "scripts": {
    "ci": "pnpm tsc --affected && pnpm build --affected && pnpm test-ci",
    "build": "turbo run build --ui=stream",
    "tsc": "turbo run tsc --ui=stream -- --noEmit",
    "test": "vitest --project unit",
    "test-ci": "CI=true turbo run test --affected --ui=stream -- --project unit/out",
    "dev": "turbo run dev --ui=tui",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "postinstall": "node ./__config/src/set-defaults.ts",
    "clear-all": "node ./__config/src/clear-all.ts",
    "refresh": "pnpm clear-all && pnpm install && pnpm tsc && pnpm build && pnpm test-ci"
  },
  "devDependencies": {
    "oxfmt": "0.24.0",
    "oxlint": "1.39.0",
    "tsdown": "0.21.4",
    "turbo": "2.7.5",
    "type-fest": "catalog:",
    "typescript": "6.0.2"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "pnpm@10.29.1"
}
