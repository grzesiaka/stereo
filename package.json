{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "stereo",
  "version": "777.777.777",
  "description": "mono stereo",
  "keywords": [],
  "license": "MIT",
  "author": "",
  "type": "module",
  "scripts": {
    "ci": "pnpm build --affected && pnpm test-ci",
    "build": "turbo run build --ui=stream",
    "test": "vitest --project unit",
    "test-ci": "CI=true turbo run test --affected --ui=stream -- --project unit/out",
    "dev": "turbo run dev --ui=tui",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "postinstall": "node ./\\$CONFIG/src/set-defaults.ts",
    "clear-all": "node ./\\$CONFIG/src/clear-all.ts",
    "refresh": "pnpm clear-all && pnpm install && pnpm build && pnpm test-ci"
  },
  "devDependencies": {
    "oxfmt": "0.24.0",
    "oxlint": "1.39.0",
    "tsdown": "0.21.4",
    "turbo": "2.7.5",
    "type-fest": "catalog:",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "packageManager": "pnpm@10.29.1"
}
